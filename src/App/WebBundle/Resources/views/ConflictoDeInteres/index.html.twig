{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ConflictoDeInteres list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Razonsocial1</th>
                <th>Ruc</th>
                <th>Razonsocial2</th>
                <th>Inicio</th>
                <th>Fin</th>
                <th>Razonsocial3</th>
                <th>Descripcion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('conflictodeinteres_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.razonsocial1 }}</td>
                <td>{{ entity.ruc }}</td>
                <td>{{ entity.razonsocial2 }}</td>
                <td>{% if entity.inicio %}{{ entity.inicio|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.fin %}{{ entity.fin|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.razonsocial3 }}</td>
                <td>{{ entity.descripcion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('conflictodeinteres_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('conflictodeinteres_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('conflictodeinteres_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
